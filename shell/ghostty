# match bg to vim/init.lua
background = 191e2d

# disable ligatures
font-feature = -calt
font-feature = -dlig
font-feature = -liga

# comfortable size on current laptop and monitor resolutions
font-size = 15

# open new tabs at $HOME, not current tab's working directory
window-inherit-working-directory = false
