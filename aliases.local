# todo
alias todo="vim ~/Dropbox\ \(Personal\)/todo"

open_irc_session_in_tmux() {
  if tmux list-sessions | grep -Fq irc; then
    tmux attach -t irc
  else
    tmux new -s irc
  fi
}

# irc
alias irc="open_irc_session_in_tmux"
