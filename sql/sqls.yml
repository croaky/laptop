lowercaseKeywords: false
connections:
  - alias: eds
    driver: postgresql
    dataSourceName: "host=127.0.0.1 port=5432 user=postgres password=secret dbname=eds_development sslmode=disable"
  - alias: webstack
    driver: postgresql
    dataSourceName: "host=127.0.0.1 port=5432 user=postgres password=secret dbname=webstack_dev sslmode=disable"
  - alias: x
    driver: postgresql
    dataSourceName: "host=127.0.0.1 port=5432 user=postgres password=secret dbname=x_dev sslmode=disable"
